[Unit]
Description=agent-bar — top-bar indicator for AI coding agents
After=graphical-session.target
PartOf=graphical-session.target

[Service]
Type=simple
ExecStart=%h/.local/bin/agent-bar run
Restart=on-failure
RestartSec=5

[Install]
WantedBy=graphical-session.target
